// Escreva uma função chamada ePaisagem que 
// recebe dois parâmetros, largura e altura
// de uma imagem (numer)
// Retorne true se a imagem estiver no modo paisagem

const ePaisagem = (largura, altura) => largura > altura; //Usando arrow function

console.log(ePaisagem(1920, 1080));